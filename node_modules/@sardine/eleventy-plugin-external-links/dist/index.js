var $1BFQ8$linkedom = require("linkedom");


const $924d1b8a657c7a85$export$326ac747d4674966 = (html)=>{
    const { document: document  } = $1BFQ8$linkedom.parseHTML(html);
    const links = [
        ...document.querySelectorAll('a')
    ];
    if (links.length > 0) links.map((link)=>{
        if (/^(https?\:\/\/|\/\/)/i.test(link.href)) {
            link.target = '_blank';
            link.setAttribute('rel', 'noreferrer');
        }
        return link;
    });
    else return html;
    return document.toString();
};


module.exports = (eleventyConfig)=>{
    eleventyConfig.namespace('external-links', ()=>{
        eleventyConfig.addTransform('external-links', (content, outputPath)=>{
            try {
                if (outputPath && outputPath.endsWith('.html')) content = $924d1b8a657c7a85$export$326ac747d4674966(content);
            } catch (error) {
                console.error(error);
            }
            return content;
        });
    });
};


//# sourceMappingURL=index.js.map
